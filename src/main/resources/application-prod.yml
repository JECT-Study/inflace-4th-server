spring:
  config:
    activate:
      on-profile: prod
  application:
    name: inflace
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION}
  refresh-expiration: 1209600000

redis:
  host: ${INFLACE_VALKEY_URL}
  port: ${INFLACE_VALKEY_PORT}
  password: ${INFLACE_VALKEY_PASSWORD:}
  database: 0

cookie:
  same-site: None
  secure: true

cors:
  allowed:
    origins:
      - https://inflace.site
